const initialState = [

]

 function projectReducer (state = initialState, action) => {
  switch (type) {

  case first:
    return { ...state, ...payload }

  default:
    return state
  }
}


export default projectReducer;